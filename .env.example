# Copy to .env (in server/) to override defaults.
# All optional — MyPortfolio auto-generates a JWT secret on first boot.

# PORT=3001
# HOST=127.0.0.1
# JWT_SECRET=change-me-to-a-long-random-string
# CLIENT_ORIGIN=http://localhost:5173
# MP_DATA_DIR=D:\VSCodeWorkspace\MyPortfolio\server\data
